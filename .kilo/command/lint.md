# Lint Command

Runs all linting checks for the project (black, isort, flake8, mypy, pylint).

## Usage

```
/lint            # Run all linting checks
/lint --auto     # Auto-fix formatting issues (black, isort)
```

## Command

./script/lint