# Setup Command

Installs project dependencies in a virtual environment.

## Usage

```
/setup              # Install dependencies
/setup --dev        # Install dev dependencies (linting, testing tools)
```

## Command

./script/setup